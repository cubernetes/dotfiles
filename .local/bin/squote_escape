#! /bin/sh -

if [ -t 0 ] && [ -n "${1}" ]; then
	while [ -n "${1}" ]; do
		printf '%s\n' "${1}" | sed "s/'/'"'"'"'"'"'"'/g; s/^/'/; s/$/'/"
		shift
	done
else
	sed "s/'/'"'"'"'"'"'"'/g; s/^/'/; s/$/'/"
fi
